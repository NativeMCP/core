[package]
name = "nmcp-exec"
description = "Governed command execution: program resolution, execution profiles, durable jobs, and the delete-verb refusal."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
categories.workspace = true
keywords.workspace = true
readme.workspace = true
homepage.workspace = true
repository.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
anyhow.workspace = true
nmcp-audit = { path = "../nmcp-audit", version = "0.1.0" }
nmcp-policy = { path = "../nmcp-policy", version = "0.1.0" }
# NMCP-SPEC-002 SB-4 (I-034): the sealed carrier the `env` injection modality receives
# material in. `nmcp-schema -> nmcp-policy` only, so the edge is acyclic.
nmcp-schema = { path = "../nmcp-schema", version = "0.1.0" }
serde.workspace = true
serde_json.workspace = true
tokio.workspace = true
uuid.workspace = true
